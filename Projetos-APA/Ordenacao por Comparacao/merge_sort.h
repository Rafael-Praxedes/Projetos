#ifndef MERGE_SORT_H
#define MERGE_SORT_H

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

//Functions
extern void Merge(int *vector, int first, int midle, int last);
extern void MergeSort(int *vector, int first, int last);

#endif